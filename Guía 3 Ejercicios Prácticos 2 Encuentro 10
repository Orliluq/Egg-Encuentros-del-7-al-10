/// Realizar un programa que lea un número entero (tamaño del lado) y a partir de él cree
/// un cuadrado de asteriscos de ese tamaño. Los asteriscos sólo se verán en el borde del
/// cuadrado, no en el interior. Por ejemplo, si se ingresa el número 4 se debe mostrar:
///		* * * *	*
///		* 		  *
///		* 		  *
///		* * * * *
/// Nota: Recordar el uso del escribir sin saltar en PseInt.

Algoritmo bucle_anidado_ej_2
	
	Definir ladoA, ladoB, n Como Entero;
	
	Escribir "Ingrese un número:";
	Leer n;
	
	Para ladoA=1 Hasta n Hacer
		Para ladoB=1 Hasta n Hacer
			Si (ladoA > 1 Y ladoA < n) Y (ladoB > 1 Y ladoB < n) Entonces;
				Escribir " " Sin Saltar;
			SiNo
				Escribir "*" Sin Saltar;
			FinSi
		FinPara
		Escribir "";
	FinPara
	
FinAlgoritmo
